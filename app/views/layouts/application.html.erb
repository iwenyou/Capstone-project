<!DOCTYPE html>
<html>
    <head>
        <title>CapstoneProject</title>
        <%= csrf_meta_tags %>

        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>

    <body>

        <div class="mdl-layout mdl-js-layout mdl-layout--fixed-header">
            <header class="mdl-layout__header">
                <div class="mdl-layout__header-row">
                    <!-- Title -->
                    <span class="mdl-layout-title" id="header_title">Wheat</span>
                    <!-- Add spacer, to align navigation to the right -->
                    <div class="mdl-layout-spacer"></div>
                    <!-- Navigation. We hide it in small screens. -->
                    <nav class="mdl-navigation">
                        <% if !current_user %>
                        <a href="/login" class="mdl-navigation__link">Sign In</a>
                        <a href="/users/new" class="mdl-navigation__link"> Sign Up</a>
                        <% else %>
                        <a class="mdl-navigation__link" href="/groceries"><i class="material-icons" id="groceries_icon">shopping_basket</i></a>

                        <a class="mdl-navigation__link" href="/users/<%= current_user.id %>">
                          Hello,
                          <%= current_user.first_name  %></a>
                        <a class="mdl-navigation__link" href="/logout">  Log Out</a>
                        <% end %>
                    </nav>
                </div>
            </header>
            <main class="mdl-layout__content">
                <div class="page-content">
                    <!-- Your content goes here -->

                    <!-- flash message -->
                    <% flash.each do |key, message| %>
                    <div>
                        <%= message %>
                    </div>
                    <% end %>

                    <div class="container-fluid">

                      <%= yield %>

                    </div>
                    <!-- flash message end -->

                </div>
            </main>
        </div>

    </body>
</html>
